// import { test as base } from '@playwright/test';

// interface Pages {
// }

// export const test = base
//   .extend<Pages>({
//     async loginPage({ page }, use) {
//       await use(new LoginPage(page));
//     },
//     async templatePage({ page }, use) {
//       await use(new TemplatePage(page));
//     },
//   })

//   });

//   export const { expect } = test;
